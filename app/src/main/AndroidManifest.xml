<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.ittepic.tpdm_revau2_luisquintanilla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CitasEditar"></activity>
        <activity android:name=".CitasInsertar" />
        <activity android:name=".PacienteEditar" />
        <activity android:name=".PacienteInsertar" />
        <activity android:name=".DoctorEditar" />
        <activity android:name=".DoctorInsertar" />
        <activity android:name=".RegistroCitas" />
        <activity android:name=".RegistroPacientes" />
        <activity android:name=".RegistroDoctores" />
        <activity android:name=".BaseDatos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>